allOf:
  - $ref: "../common/EnvelopeBase.yaml"
  - type: object
    required:
      - data
    properties:
      data:
        type: array
        description: Country-level summary rows (currently empty on staging).
        items:
          type: object
          additionalProperties: true
